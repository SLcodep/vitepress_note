---
title: js基础部分
date: 2025-11-28 17:58:41
permalink: /qianduan/基础
categories:
  - 前端
  - javascript
tags:
  - javascript
---

<CollapsibleToc 
  title="快速导航" 
  :defaultOpen="true" 
  :minLevel="2" 
  :maxLevel="3" 
/>

::: details 💡 内容大纲

- 理解DOM操作
- 事件处理、事件冒泡、事件委托
- JS有多少种继承方式

:::




## settimeout和setIntercal的陷阱
::: details 💡 内容详情

`setTimeout`和`setInterval`是 Window/Worker 接口提供的定时器API

**`setTimeout:`** 迟执行一次（一次性定时器）
- 作用:设置一个「延迟时间」，到达时间后仅执行一次指定的函数/代码片段。
- 延迟执行非紧急任务、模拟异步操作、防抖
**`setInterval:`** 重复执行（周期性定时器）
- 周期性更新数据、轮播图自动切换、实时监控

::: code-group
```js [settimeout实现防抖]
let searchTimer;
input.addEventListener("input", (e) => {
  clearTimeout(searchTimer); // 清除上一次的定时器
  searchTimer = setTimeout(() => {
    fetchSearch(e.target.value); // 输入停止500ms后执行搜索
  }, 500);
});
```

``` js [setinterval]
// 60秒倒计时
let seconds = 60;
const timer = setInterval(() => {
  seconds--;
  if (seconds <= 0) {
    clearInterval(timer);
    console.log("倒计时结束");
  }
  console.log(`剩余：${seconds}秒`);
}, 1000);
```
:::

### 

